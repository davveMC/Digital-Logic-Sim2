// KeyActions, To add another action just add 
// it to the enum and everything else will just work
public enum KeybindingActions
{
    Delete,
    ZoomIn,
    ZoomOut,
    RotateLeft,
    RotateRight,
    Copy,
    Paste,
    StackAdd,
    StackRemove,
    None
}
